const add = (a,b) => a+b;
const substract = (a,b) => a-b;
const multiply = (a,b) => a*b;
const divide = (a,b) => a/b;

const methods = {
    add,
    substract,
    multiply,
    divide,
};

module.exports = methods;